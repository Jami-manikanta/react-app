import styled from "@emotion/styled";
import tw from "tailwind.macro";

const ECommereceBody = styled.div`
    ${tw`w-full p-6 `}
`;
//const ECommerce=styled.div`${tw`h-screen`}`

export { ECommereceBody };
