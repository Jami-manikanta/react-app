import styled from "@emotion/styled";
import tw from "tailwind.macro";

const SortByDropDownButton = styled.select`
    ${tw`rounded-md bg-white border border-gray-400 hover:border-gray-700 focus:outline-none `}
`;

export { SortByDropDownButton };
