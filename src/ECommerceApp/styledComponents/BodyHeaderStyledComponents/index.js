import styled from "@emotion/styled";
import tw from "tailwind.macro";

const ECommerceBodyHeader = styled.div`
    ${tw` w-full flex justify-between items-center my-4 border border-solid border-black`}
`;
//const SizesHeading=styled.div`${tw`my-4 font-bold`}`
export { ECommerceBodyHeader };
